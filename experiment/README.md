# experiment

Legacy prototype area kept for reference.

## Status

This folder is not the primary experiment path anymore.

Current active experiment modules are:

- `Few_shot_learning/` for few-shot adaptation
- `Evaluation/` for heuristic vs pretrained-policy comparison

## Recommendation

Use the newer modules instead of building new work on top of this folder.

Main replacements:

- legacy few-shot experiments -> `Few_shot_learning/`
- legacy comparison/evaluation scripts -> `Evaluation/`

## Why this folder still exists

It may still contain older prototypes or exploratory code that can be useful for reference, but it should not be treated as the authoritative implementation path for current experiments.
